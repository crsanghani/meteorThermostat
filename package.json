{
  "name": "meteorThermostat",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "chimp --ddp=http://localhost:3000 --path=tests --jasmine || exit 0"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0",
    "babel-runtime": "6.18.0"
  }
}
